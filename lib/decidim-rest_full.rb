# frozen_string_literal: true

# Stub so `require "decidim-rest_full"` (Bundler default for gem name) loads the engine.
# Required for the engine to register and expose rake tasks (e.g. decidim_rest_full:install:migrations).
require "decidim/rest_full"
