label: Add an endpoint
link:
  type: generated-index
  slug: /dev/add-endpoint
  title: Add an endpoint
  description: |
    RestFull extension guide for Decidim gem authors. Start with Recipe, then open topic pages only when you need them.
    Integrators: use Operating the API instead.
